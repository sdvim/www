---
# Metadata comes from _data/metadata.json
permalink: "{{ metadata.feed.path }}"
eleventyExcludeFromCollections: true
---
<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
	<title>{{ metadata.title }}</title>
	<subtitle>{{ metadata.feed.subtitle }}</subtitle>
	{% set absoluteUrl %}{{ metadata.feed.path | url | absoluteUrl(metadata.url) }}{% endset %}
	<link href="{{ absoluteUrl }}" rel="self"/>
	<link href="{{ metadata.url }}"/>
  {% set latestWork = works | last %}
	<updated>{{ latestWork.date }}</updated>
	<id>{{ metadata.feed.id }}</id>
	<author>
		<name>{{ metadata.author.name }}</name>
		<email>{{ metadata.author.email }}</email>
	</author>
	{%- for work in works | reverse %}
	{% set absoluteWorkUrl %}{{ work.url | url | absoluteUrl(metadata.url) }}{% endset %}
	<entry>
		<title>{{ work.title }}</title>
		<link href="{{ absoluteWorkUrl }}"/>
		<updated>{{ work.date }}</updated>
		<id>{{ absoluteWorkUrl }}</id>
		<content type="html">{{ work.description | htmlToAbsoluteUrls(absoluteWorkUrl) }}</content>
	</entry>
	{%- endfor %}
</feed>
